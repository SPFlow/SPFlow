before_install:
  - sudo apt-get update
  - sudo apt-get install -y libgl1-mesa-glx
language: python
python:
  - "3.5"
  - "3.6"
# command to install dependencies
install:
  - pip install -r requirements.txt
  - pip install tensorflow
# command to run tests
script: python3 -m unittest discover src